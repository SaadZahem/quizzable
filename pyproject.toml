[project]
name = "quizzable"
version = "0.1.0"
description = "Website for sharing quizzes"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "nicegui>=3.14.0",
    "tortoise-orm>=1.1.7",
]
